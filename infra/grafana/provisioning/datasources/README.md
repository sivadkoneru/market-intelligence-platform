# Datasource provisioning

Provisioned Grafana datasources for the local observability stack.

Portfolio project only. No financial advice and no real trades.

## Files

- `datasources.yaml` - Elasticsearch log store plus the pinned HTTP JSON
  datasource used to query Druid SQL
