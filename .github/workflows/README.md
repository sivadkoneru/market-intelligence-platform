# GitHub Actions Workflows

CI workflows in this directory mirror the local quality gates:

- install the pinned Python development dependencies
- validate the Docker Compose configuration
- run Ruff
- run Pytest
- build the Docker Compose service images

